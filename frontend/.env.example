# 로컬 개발용 (frontend/.env 로 복사)
PORT=5173
BACKEND_PORT=8000
BASE_PATH=./

# Vercel 배포: 대시보드에 설정하지 않아도 check-env 기본값으로 빌드됩니다.
# (선택) BASE_PATH=/
# (선택) BACKEND_PORT=8000

# 모바일 지갑(WalletConnect) 연결 — https://cloud.reown.com 에서 Project ID 발급
VITE_WALLETCONNECT_PROJECT_ID=
